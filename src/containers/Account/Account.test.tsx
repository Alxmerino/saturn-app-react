import React from 'react';

test("Shows User's first and last name", async () => {
  expect(1).toBe(1);
});

// shows button to add jira integration
// shows jira integration modal
// shows jira integration connected
// shows re-login button on session expired
