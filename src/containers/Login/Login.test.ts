import React from 'react';

test('shows disabled submit button when log in form is empty', async () => {
  expect(1).toBe(1);
});

// shows enabled submit button when login form is filled
// can log in successfully
// shows errors on failed login
// shows link to sign up form
