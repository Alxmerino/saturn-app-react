import React from 'react';

test('shows disabled submit button when sign up form is empty', async () => {
  expect(1).toBe(1);
});

// shows enabled submit button when sign up form is filled
// can sign up successfully
// shows errors on failed sign up
// shows link to log in form
