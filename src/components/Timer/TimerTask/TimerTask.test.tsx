import React from 'react';

test('shows task timer running', async () => {
  expect(1).toBe(1);
});

// can pause task timer
// can start a new task timer
// can edit task title
// can reset task timers
// can delete task
// shows "Log Time" option
// can log time
