import React from 'react';

test('shows add button disabled if task title field empty', async () => {
  expect(1).toBe(1);
});

// adds task with default project color
// adds task with selected project color
